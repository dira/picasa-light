.navigation
  %a{ :href => '/' } home

%h1
  =h @user[:name]
  has
  = @user[:albums].size
  albums:

%ol.albums
  - @user[:albums].each do |album|
    %li
      %a{ :href => "/#{URI.encode(params[:username])}/#{album[:id]}/#{album[:uri]}", :title => album_title(album) }
        =h album[:title]

.navigation
  You can view these albums on
  %a{ :href => @user[:link] } Picasa
.navigation
  %a{ :href => '/' } home
  = "/ #{h @user[:name]}"
